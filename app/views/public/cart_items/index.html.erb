<div class="container">
<div class="row">
<div class="col-lg-4">
<h4>ショッピングカート</h4>
 </div>
<div class="col-lg-4"> </div>
  <div class="col-lg-4 text-right">
    <%= link_to "カートを空にする",destroy_all_path(@cart_items),method: :delete, class: " btn btn-sm btn-danger" %>
    </div>
    </div>

<table class ="table table-bordered">
  <thead class="table-secondary">
      <th>商品名</th>
      <th>値段</th>
      <th></th>
      <th>小計</th>
      <th></th>
    </thead>
  <today>

<% @cart_items.each do |cart_item| %>
<%= form_with model: cart_item,url: cart_item_path(cart_item) do |f| %>
 <tr>
<td><%= image_tag cart_item.item.get_image_id(100,100) %>
<%= cart_item.item.name %></td>
<td><%= (cart_item.item.price*1.08).floor %>円</td>
<td><%= f.select :amount,*[1..10] %>
<%= f.submit "更新",class: "btn btn-sm btn-success" %></td>
<td><%= cart_item.amount*(cart_item.item.price*1.08).floor %>円</td>
<td><%= link_to "削除", cart_item_path(cart_item),method: :delete,"data-confirm" => "Are you sure?", class: " btn btn-sm btn-danger" %></td>
 </tr>
<% end %>
<% end %>
  </today>
   </table>
   <div class="row">
     <div class="col-lg-4">
<%= link_to "買い物を続ける", root_path,class: "btn btn-sm btn-primary" %>
 </div>
<div class="col-lg-4"> </div>
  <div class="col-lg-4 text-right">
   合計金額
<%= @total.floor %>円

    </div>
</div>
<br>
<div class="row">
  <div class="col-lg-4 text-right"> </div>
  <div class="col-lg-4 text-center">
    <% if @total == 0 %>
    <% else %>
<%= link_to "情報入力に進む", new_order_path,class: "btn btn-success btn-sm btn-block" %><% end %>
 </div>
  </div>
</div>