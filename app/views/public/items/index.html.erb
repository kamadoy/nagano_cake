<h1>商品一覧(全<%= Item.count %>件)</h1>
<% @items.each do |item| %>
<%= image_tag item.get_image_id(200,200) %><br>
<%= link_to item_path(item) do %>
<%= item.name %><br>
<%= item.price %>円
<% end %><br>
<% end %>
