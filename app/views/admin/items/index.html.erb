      <li>
        <%= link_to "アイテム追加", new_admin_item_path %>
      </li>
<h1>商品一覧</h1>
 <% @items.each do |item| %>
　<h4>商品id</h4>
　<%= item.id %>
　<h4>商品名</h4>
　<%= link_to admin_item_path(item) do %>
　<%= item.name %>
　<% end %>
　<h4>ジャンル</h4>
　<%= item.genre.name %>
　<h4>税抜き価格</h4>
　<%= item.price %>円
　<h4>販売ステータス</h4>
　<%= item.is_active ? '販売中':'販売停止中' %>
<% end %>